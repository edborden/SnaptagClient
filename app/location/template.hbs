<div class="index-bg">
  {{#content-panel full=true styled=false}}
    <h1>{{i-fa icon="map-marker" size="1x"}}
    Use Location?</h1><br><br><br>
    <h4>In order to show you other members in your area, SSSSS would like to access your location information from your device. This location information won't be stored and we won't share it with anyone.</h4><br>
    {{#if waiting}}
      {{i-fa size="4x" icon="cog" class="fa-spin"}}
    {{/if}}
    {{#if error}}
      <h4 class="color-red">{{error}}</h4>
    {{/if}}
    <br><br>
    <button class="btn btn-lg btn-primary" {{action 'confirm'}}disabled={{waiting}}>
      Confirm
    </button>
    <button class="btn btn-lg btn-primary" {{action 'cancel'}}>
      Cancel
    </button>
  {{/content-panel}}
</div>