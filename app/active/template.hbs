{{active-map bounds=initialBounds initialCenter=center initialZoom=zoom model=model setModal=(action send 'setModal')}}

{{partial 'me-button'}}

{{circle-button class="second" click=(action send "setModal" "web-interface" "partial") icon="users"}}