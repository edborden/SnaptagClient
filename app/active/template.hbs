{{#snaptag-map bounds=initialBounds initialCenter=center initialZoom=zoom}}
  {{tile-layer url='https://{s}.tiles.mapbox.com/v3/edborden.i7622aec/{z}/{x}/{y}.png'}}
  {{me-marker}}
  {{#each model as |location|}}
    {{#user-marker imageUrl=user.smallpic lat=location.lat lng=location.lng}}
      {{location.user.name}}, {{moment-from-now location.createdAt}}.
    {{/user-marker}}
  {{/each}}
{{/snaptag-map}}

{{active-interface}}